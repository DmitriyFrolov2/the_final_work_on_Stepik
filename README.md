# the_final_work_on_Stepik
## Требования
- Python 3.12+

pytest -v --tb=line --language=en test_main_page.py - эта команда для запуска тестов.
--tb=line указывает, что нужно выводить только одну строку из лога каждого упавшего теста
